# Regex Sandbox

A diagnostic tool for testing regular expressions. It provides live syntax highlighting and match targeting against arbitrary user text to help experiment with and fine-tune JS RegExp strings in a safe sandbox environment.
