# Theme Switcher

A dynamic visual theme recalibration tool featuring local storage memory persistence. It demonstrates how to toggle CSS variables across an entire application document and cache the user's preference in their browser.
