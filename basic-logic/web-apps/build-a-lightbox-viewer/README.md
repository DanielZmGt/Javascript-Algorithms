# Lightbox Viewer

A lightweight gallery projection system for rendering high-resolution visual data. This implements a common image gallery pattern where clicking a thumbnail opens a full-screen, focused view of the image (a lightbox) with navigation controls.
